argo-cd:
  # federate ids, act as a connector
  dex:
    enabled: false
  server:
    extraArgs: ["--insecure"]
    config:
      repositories: |
        - type: helm
          name: argo-cd
          url: https://argoproj.github.io/argo-helm